{% extends 'base.html.twig' %}

{% block title %}Modifier notification{% endblock %}

{% block body %}
<turbo-frame id="main-frame">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Modifier la notification</h2>
        <a href="{{ path('app_notifications_index') }}" class="btn btn-outline-secondary btn-sm">Retour</a>
    </div>

    {{ include('notifications/_form.html.twig', {'action': path('app_notifications_edit', {'id': notification.id}), 'button_label': 'Enregistrer'}) }}

    {{ include('notifications/_delete_form.html.twig') }}
</turbo-frame>
{% endblock %}
