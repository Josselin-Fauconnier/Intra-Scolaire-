<form method="post" action="{{ path('app_promotion_users_delete', {'id': promotion_user.id}) }}" onsubmit="return confirm('Supprimer cette affectation ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promotion_user.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
