{% extends 'base.html.twig' %}

{% block title %}PromotionUsers{% endblock %}

{% block body %}
    <h1>PromotionUsers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion_user.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promotion_users_index') }}">back to list</a>

    <a href="{{ path('app_promotion_users_edit', {'id': promotion_user.id}) }}">edit</a>

    {{ include('promotion_users/_delete_form.html.twig') }}
{% endblock %}
