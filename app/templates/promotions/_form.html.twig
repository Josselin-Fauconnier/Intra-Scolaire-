{{ form_start(form, {'action': app.request.requestUri, 'attr': {'class': 'dashboard-form'}}) }}
    <div class="dashboard-form-shell">
        <div class="dashboard-form-intro">
            <span class="pill">Promotion</span>
            <p class="dashboard-note">Associe un professeur et les projets relies a cette promotion.</p>
        </div>

        {% if form_errors(form) %}
            <div class="dashboard-flash dashboard-flash-danger">
                {{ form_errors(form) }}
            </div>
        {% endif %}

        <div class="dashboard-form-fields">
            {{ form_widget(form) }}
        </div>

        <div class="dashboard-form-actions">
            <button class="btn btn-primary">{{ button_label|default('Enregistrer la promotion') }}</button>
        </div>
    </div>
{{ form_end(form) }}
