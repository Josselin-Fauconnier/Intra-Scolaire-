<form method="post" action="{{ path('app_promotions_delete', {'id': promotion.id}) }}" onsubmit="return confirm('Supprimer cette promotion ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promotion.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
