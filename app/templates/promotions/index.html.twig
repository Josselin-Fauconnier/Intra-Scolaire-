{% extends 'base.html.twig' %}

{% block title %}Promotions index{% endblock %}

{% block body %}
    <h1>Promotions index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Professeur</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for promotion in promotions %}
            <tr>
                <td>{{ promotion.id }}</td>
                <td>{{ promotion.name }}</td>
                <td>{{ promotion.professor.firstname }} {{ promotion.professor.lastname }}</td>
                    <a href="{{ path('app_promotions_show', {'id': promotion.id}) }}">show</a>
                    <a href="{{ path('app_promotions_edit', {'id': promotion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_promotions_new') }}">Create new</a>
{% endblock %}
